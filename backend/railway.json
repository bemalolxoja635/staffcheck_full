{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py create_admin && gunicorn staffcheck.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/settings/public/",
    "restartPolicyType": "ON_FAILURE"
  }
}
