[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python manage.py migrate --noinput && python manage.py create_admin && gunicorn staffcheck.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
